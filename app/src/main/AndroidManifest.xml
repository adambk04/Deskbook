<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deskbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainFragmentActivity"></activity>
        <activity
            android:name=".BookSlotConfirmationDialog"
            android:theme="@style/Theme.MaterialComponents.Light.Dialog.Alert" />
        <activity android:name=".WorkspaceBookSlotActivity" />
        <activity android:name=".WorkspaceListActivity" />
        <activity
            android:name=".SortWorkspaceDialog"
            android:theme="@style/Theme.MaterialComponents.Light.Dialog.Alert" />
        <activity android:name=".ProfileFragment" />
        <activity android:name=".SetupProfileActivity" />
        <activity android:name=".HomeFragment" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>